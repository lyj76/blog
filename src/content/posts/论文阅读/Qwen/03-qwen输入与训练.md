---
title: Qwen 输入与训练：词表、右移对齐与交叉熵
published: 2026-08-08
description: 152K 词表的信息密度、因果右移一位对齐、交叉熵解析梯度推导，以及 SFT 为什么用 -100 掩码。
tags: [论文阅读, Qwen, 训练, 词表, 交叉熵, LossMasking]
category: 论文阅读
---

# Qwen 输入与训练：词表、右移对齐与交叉熵

> 承接 [[论文阅读/qwen/01-qwen主线]] 与 [[论文阅读/qwen/02-qwen-decoder-block]]：01 给了端到端链路，02 拆了单层算子。本篇回答训练侧的问题——**模型学的是什么**：152K 词表怎么选、输入标签怎么对齐、交叉熵的梯度为什么这么干净、SFT 为什么必须掩码。索引见 [[论文阅读/qwen/00-qwen阅读地图]]。

## 词表为什么选 152K

Qwen 用 Byte-level BPE 词表，规模 151,936（约 152K）——相比 LLaMA-1 的 32K 扩大了近 5 倍。

词表大小直接决定 Token 的信息密度。以中文为例：32K 词表下，一个常用词往往被拆成 4~6 个 Byte Token；152K 词表下，大部分高频词作为单个整 Token 存在。平均一个中文字符消耗的 Token 数从约 1.5 降到 0.65~0.8。

Token 数少带来两个连锁收益：

- **等效上下文变长**：同样的 $T$ 个 Token 装进更多自然语言，注意力开销是 $O(T^2)$，少一个 Token 省的是平方级的计算；
- **语义先验更密**：每个 Token 承载更完整的语义单位，模型不需要从字节碎片里重新组装词义。

代价是 Embedding 和 LM Head 变大——两者形状都是 $V \times d_{\text{model}} = 151936 \times 896$，这就是 01 里 `tie_word_embeddings` 存在的理由。词表越大，这两张表越贵，小模型必须靠权重绑定来摊平。

## 右移一位：因果自回归的标签对齐

因果语言模型的优化目标是：**给定前 $i$ 个 Token，最大化第 $i+1$ 个 Token 的概率**。

实现上靠输入和标签的错位完成。给定序列 $t = (t_1, \dots, t_T)$：

$$
\begin{aligned}
\text{Inputs}: \quad X &= (t_1, t_2, \dots, t_{T-1}), \\
\text{Labels}: \quad Y &= (t_2, t_3, \dots, t_T).
\end{aligned}
$$

```text title="shift_right.txt"
时间步 (Step):     1        2        3       ...      T-1
输入序列 (Inputs): [今天]    [天气]    [真好]   ...      [适合]
                     │        │        │               │
                  [ 24 层 Decoder Block (因果掩码阻断未来) ]
                     │        │        │               │
预测分布 (Logits): z_1      z_2      z_3     ...      z_{T-1}
                     │        │        │               │
目标标签 (Labels): [天气]    [真好]    [，]     ...      [散步]
```

位置 $i$ 的预测目标是 $t_{i+1}$。由于因果掩码保证位置 $i$ 读不到 $j > i$ 的信息，模型只需一次前向就能得到全部 $T-1$ 个位置的 Logits，一次反向更新全部梯度——这就是 01 里"训练并行"的具体形态。

## 交叉熵：梯度为什么这么干净

模型在位置 $i$ 输出 Logits $z_i \in \mathbb{R}^{V}$，经 Softmax 得到预测分布：

$$
p_{i,j} = \frac{e^{z_{i,j}}}{\sum_{k=1}^{V} e^{z_{i,k}}}.
$$

损失是真实类别 $y_i$ 的负对数似然：

$$
\mathcal{L}_i = -\log p_{i, y_i} = \log \sum_{k=1}^{V} e^{z_{i,k}} - z_{i, y_i}.
$$

对 Logit 分量 $z_{i,j}$ 求偏导，中间两步会消掉：

$$
\frac{\partial \mathcal{L}_i}{\partial z_{i,j}} 
= \frac{e^{z_{i,j}}}{\sum_k e^{z_{i,k}}} - \mathbb{I}(j = y_i) 
= p_{i,j} - q_{i,j}.
$$

其中 $q_i$ 是真实标签的 One-Hot 向量。**交叉熵对 Logit 的梯度 = 预测概率 − One-Hot 标签**。这个形式的信息量很大：

- 预测命中真实类别（$p \to 1$）时，梯度趋近 0——模型已对，不需要更新；
- 预测偏离时，梯度与误差成正比——偏离多少，推多少，没有饱和区。

为什么不能换成 MSE？MSE 的梯度要过 Softmax 导数 $p_j(\delta_{jk} - p_k)$，在 $p \to 0$ 或 $p \to 1$ 时都趋近 0——**概率空间越接近正确，梯度越消失**。交叉熵的 $-\log$ 和 Softmax 的指数在求导时互相抵消，把这条消失链拆掉了。这是它成为语言模型默认损失的根本原因。

## SFT 掩码：为什么 Prompt 区域填 -100

指令微调（SFT）的语料是 Prompt（系统提示 + 用户输入）与 Target（期望输出）的拼接。问题在于：**Prompt 占了序列长度的 70%~90%，如果不掩码，模型大部分梯度都花在"死记硬背问题文本"上**。

两个后果：一是参数容量浪费——有限的容量被耗在复读题干上；二是破坏自回归先验——强行拟合外部给定的文本，容易诱发复读与语言退化。

PyTorch 的 `nn.CrossEntropyLoss(ignore_index=-100)` 按指示函数加权：

$$
\mathcal{L} = \frac{1}{\sum_i \mathbb{I}(y_i \ne -100)} \sum_i \mathbb{I}(y_i \ne -100) \cdot (-\log p_i[y_i]).
$$

```text title="loss_mask.txt"
输入序列 (input_ids):
[<|im_start|>, system, ..., <|im_start|>, assistant, \n, 央行, 宣布, 降准, <|im_end|>, <pad>]
                                                          ▲
                                                 从这里开始计算 Loss
目标标签 (labels):
[    -100,      -100,  ...,     -100,      -100,    -100, 央行, 宣布, 降准, <|im_end|>, -100 ]
 └──────────────────────┬──────────────────────────┘ └───────────┬─────────────┘ └───┬──┘
              Prompt 区域全部 -100 (不产生梯度)           Target 区域正常计分      Padding
```

标签为 $-100$ 的位置梯度直接置零，不参与任何参数回传。**注意这个掩码不是可选优化，是 SFT 的硬性契约**——不掩码的模型会学成"复读机"。

## 边界

- **152K 词表不是越大越好**：收益递减点是"常用词已覆盖"，继续扩表只增加 Embedding/LM Head 的体积和训练成本。Qwen 的 152K 是中文/代码压缩率与参数成本之间的平衡点。
- **"交叉熵梯度 = 概率差"只对 CE 成立**：这是 Softmax + NLL 的组合特性，换成其他损失（如标签平滑会改公式但保留思想，换成 MSE 则完全不同）。
- **-100 是 PyTorch 的约定，不是数学必然**：任何不出现在真实词表里的哨兵值都行，`ignore_index` 只是它的实现。

推理侧的物理瓶颈与 KV Cache 显存建模见 [[论文阅读/qwen/04-qwen推理与采样]]。config 字段到 PyTorch 源码的映射见 [[论文阅读/qwen/05-qwen模型文件与代码]]。
